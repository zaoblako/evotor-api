import {Express} from "express";
import UserController from "../controllers/UserController";

export default class UserRoute {
    constructor(app: Express) {
        // app.route("/user/").get(UserController);
    }
}